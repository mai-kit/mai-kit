version: 2

updates:
  # pnpm workspace 必须从根目录统一更新，不能单独以 /docs 作为更新目录。
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "04:00"
      timezone: Asia/Shanghai
    open-pull-requests-limit: 5
    # 大版本升级需要同步评估运行时与工具链，留给计划性迁移；安全更新不受此规则影响。
    ignore:
      - dependency-name: "*"
        update-types:
          - version-update:semver-major
    groups:
      javascript-non-major:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
      javascript-security:
        applies-to: security-updates
        patterns:
          - "*"
    commit-message:
      prefix: chore
      include: scope

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "04:00"
      timezone: Asia/Shanghai
    groups:
      github-actions:
        patterns:
          - "*"
    commit-message:
      prefix: chore
      include: scope
